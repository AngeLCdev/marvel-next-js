const zIndex = require('./_z-index')
const Typography = require('./_typography')
const Colores = require('./_colors')

const variables = { ...zIndex, ...Typography, ...Colores }

module.exports = variables